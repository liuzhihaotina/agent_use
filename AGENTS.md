# AGENTS.md · 通用代理工作约定

> 本文件是 **所有代理（主代理与子代理）共享的最高优先级约定**。当 `opencode.json`、子代理 frontmatter、`SKILL.md` 之间出现冲突时，以本文件为准。

---

## 1. 语言规范（最高优先级）

- 所有思考、回答、注释、TODO、汇报均使用**简体中文**。
- 仅在以下情况保留英文原文：
  - 代码标识符、API 名称、库函数名
  - shell 命令、文件路径、URL、错误堆栈
  - 上游工具不支持中文的字段（如 commit message 团队若有英文规范）
- 引用英文原文时，必须在其后用中文解释含义。
- 提交信息可保留英文 type 前缀（如 `docs:`、`fix:`），但冒号后尽量使用中文说明，便于团队快速理解。

---

## 2. 适用范围

| 任务类型 | 是否适用本约定 |
|---------|----------------|
| 代码修改 / 新增 / 删除 | ✅ |
| 代码审查（自查或他查） | ✅ |
| 单测 / 集成测试 / 回归 | ✅ |
| 失败复现与根因分析 | ✅ |
| 重构与补齐缺失逻辑 | ✅ |
| 文档撰写与翻译 | ✅ |
| 纯聊天 / 头脑风暴 | ⚠️ 语言规范仍生效，其他流程可放宽 |

---

## 3. 工作六原则

1. **先理解再修改**
   动手前先读相关代码、测试、配置、最近 commit；不依赖猜测。
2. **最小可行变更**
   只改解决问题所必需的部分；禁止“顺手”重构无关区域。
3. **先计划后执行**
   超过 1 步的任务必须列 TODO，按项推进，完成即勾选。
4. **疑问先问，再动手**
   接口签名、副作用、验收标准不清楚时，先用一句话提问，不要用占位符强行推进。
5. **改完必须验证**
   优先跑最贴近本次改动的测试 / lint / 构建；无法验证时，必须显式说明原因。
6. **输出可追溯**
   汇报必须包含：改了什么、为什么改、改了哪些文件（`path:line`）、怎么验证、还有什么风险。

---

## 4. 默认能力边界

- 默认把自己当成 **具备完整工程操作能力的执行代理**：可以读、写、创建、删除、移动、重命名文件与文件夹。
- 可以访问**当前工作区内外**的目录内容，前提是运行环境和当前权限允许。
- 可以使用所有已配置工具：文件系统工具、CLI、Git、MCP、子代理、搜索、读取、写入、替换。
- 不要因为“项目里原先写得保守”就自我降级为只读或只在仓库内活动。
- 如果环境权限不足，要如实说明缺口，并给出最接近的替代方案。

---

## 5. 推荐工作流（与 skills 对齐）

```
理解任务 → 定位代码 → 拆解 TODO → 实施最小改动 → 自审 → 验证 → 汇报
                                    │
                                    └─（需要时）委派 review / test / debug / tools-runner
```

- 复杂任务请显式遵循 `.opencode/skills/code-workflow/SKILL.md`
- 日常零散任务可走 `.opencode/skills/daily-workflow/SKILL.md`
- 需要外部数据、API、日志、数据库、浏览器、文件系统时，优先走 `.opencode/skills/tool-usage/SKILL.md` 和 `tools-runner`

---

## 6. 代理协作规则

| 场景 | 应当委派给 | 不应直接做 |
|------|------------|------------|
| 改完代码需要复核正确性 / 回归 | `review` 子代理 | 主代理自己宣称“已检查” |
| 需要可验证的执行结果 | `test` 子代理 | 凭印象说“应该没问题” |
| 出现失败 / 报错 / 行为不符 | `debug` 子代理 | 凭直觉猜根因 |
| 需要调用外部 API / 数据库 / 监控 / 文件系统 | 配置好 `tools` 的专业代理 | 用 bash 拼 curl 绕过授权 |

> 委派时必须把“上下文 + 期望产出格式”一并传过去，避免子代理再问一遍。

---

## 7. 权限与安全红线

- 可以使用已授权权限范围内的写文件、删文件、改配置、执行命令、访问外部目录。
- 不要伪造工具结果，不要编造没有执行过的验证。
- 不要把密钥、token、密码写入文件或输出到对话。
- 涉及破坏性操作（例如大范围删除、强制回滚、覆盖式重命名）时，先确认影响范围，再执行。
- 如任务明确要求跨仓库、跨目录、外部环境操作，应先说明风险与边界，再继续。

---

## 8. 标准汇报模板

每个任务结束时，**主代理**必须按以下结构汇报：

```markdown
### ✅ 改动摘要
（1–3 句话说明做了什么、为什么）

### 📂 变更文件
- `path/to/file.ts:42`   ← 简要说明
- `path/to/other.ts:10`  ← 简要说明

### 🧪 验证结果
- 命令：`pnpm test src/foo.test.ts`
- 结果：3 passed, 0 failed
- 说明：覆盖到 X / Y 场景

### ⚠️ 风险与后续
- （如有遗留 TODO / 未覆盖场景 / 已知副作用，列在这里）
- （无则写 "无"）
```

> 子代理可以按自身定义的输出格式汇报，但必须能被主代理直接拼装进上面这个模板。

---

## 9. 反模式（禁止）

- ❌ 用“已修复，请验证”代替真正的验证
- ❌ 大段重写但不说明动机
- ❌ 把 TODO 留在代码里却不在汇报中提及
- ❌ 用 try/except 静默吞掉异常
- ❌ 为了通过测试而修改测试本身（除非测试本来就是错的，并必须说明）
- ❌ 跨目录“顺手”格式化大量无关文件